---
name: e2e-setup
description: uapp_e2e E2Eテスト環境をこのプロジェクトにセットアップ（または再セットアップ・修復）する。「E2E環境をセットアップして」「E2Eの設定を直して」等で使用。
---

# E2E環境のセットアップ

`uapp_e2e/SETUP.md`（AI向けセットアップランブック）を読み、その手順を上から実行する。

- 環境検出（Unityバージョン/package/画面向き/NGUI有無/AVD/エディタ）は**必ず実物のファイル・コマンドから読む**（推測しない）
- プロジェクト設定の変更は最小限・可逆に。迷う変更は実行前にユーザーへ提示
- 最後に必ずビルド→ping疎通まで実際に動かして検証し、変更ファイル一覧を報告する

キットがまだ無い場合（GitHub URLやzipを渡された場合）は、ユーザーの許可を得て取得・展開し、
**展開先の `SETUP.md` を先に読んでその手順に従う**。installer の置き場は入手形態で変わるので
（配布 zip と公開リポの clone は**直下**、開発リポジトリは `scripts\` 配下）、
**ここでパスを決め打ちにしない** ― SETUP.md の冒頭に「直下に `install-to-project.ps1` が
あるかで見分ける」判別手順がある。
